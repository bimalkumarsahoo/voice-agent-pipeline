[project]
name = "voice-agent-pipeline"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
