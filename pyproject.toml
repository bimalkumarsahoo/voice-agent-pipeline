[project]
name = "voice-agent-pipeline"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "sounddevice>=0.5.5",
    "websockets>=12.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
